[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not integration and not sql_legacy"
markers =
    integration: reserved marker for external integration tests
    sql_legacy: tests tied to retired SQL backend behavior
