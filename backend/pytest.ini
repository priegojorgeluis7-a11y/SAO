[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not integration"
markers =
    integration: marks tests requiring PostgreSQL (skipped in unit suite; run with -m integration)
