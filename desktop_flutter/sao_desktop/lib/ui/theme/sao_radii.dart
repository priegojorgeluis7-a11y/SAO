// lib/ui/theme/sao_radii.dart

/// Border radius centralizado de SAO
/// ⚠️ NO uses valores numéricos directos, usa SOLO estos tokens
class SaoRadii {
  SaoRadii._();

  // ============================================================
  // BORDER RADIUS
  // ============================================================
  static const double sm = 8.0;   // Small (chips, badges)
  static const double md = 12.0;  // Medium (buttons, inputs, cards)
  static const double lg = 14.0;  // Large (dialogs, bottom sheets)
  static const double xl = 16.0;  // Extra large (destacados)
  static const double full = 9999.0; // Circular completo
}
